\*\*我的应用启动时会出现短暂黑屏闪烁（约 0.3 秒），随后主窗口才正常显示。\*\*



\- 技术栈：Python 3.xx + PySide6 x.x，Windows 11，Nuitka 打包版运行（源码运行同样复现 / 仅打包后复现）

\- 现象细节：双击 exe 启动后，屏幕先整体变黑一下（或仅应用窗口区域闪黑），主界面才出现；黑闪发生在窗口显示前

\- 复现方式：每次启动必现；直接运行 `main.py` 也能复现

\- 已尝试的排查：设置过窗口背景色、延迟 `show()`、关闭高 DPI 缩放，均无效

\- 期望效果：启动时无黑闪，直接显示主窗口

