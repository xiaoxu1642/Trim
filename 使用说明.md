# Trim 使用说明

> 由小旭个人开发制作，仅供个人测试交流使用。Windows 11 清理与优化工具，Fluent Design 原生风格。
> 版本 **2.0** · 适用 Windows 11 22H2 及以上（已在 27H2 Build 29648 验证）· 需要 PowerShell 7
>
> 本文件是应用内「设置 → 使用说明」弹窗的独立数据源，与项目协作文档（AGENTS.md）相互独立。

---

## 一、近期界面调整（设置页整合）

- **窗口材质**卡移除「无材质」选项卡：与材质总开关功能重复，**关闭总开关即为无材质观感**（记忆所选材质，重开即恢复）。
- 「皮肤：液态玻璃」与背景磨砂合并为 **背景模糊度** 滑块（位于「背景图片」卡内），0–100% 无级调节：0% 为经典不透明面板，100% 为完整液态玻璃磨砂；旧皮肤偏好自动迁移（液态玻璃 = 100%）。
- 「主题选择」卡内新增两个分区：**预设主题选择**（原「预设背景」下拉：无 / 极光 Aurora / 落日 Sunset / 石墨 Graphite）与**主题颜色选择**（原「颜色选择」卡的强调色）。
- 「鼠标拖尾」与「切换动效」合并为 **动效设置** 卡，下分**鼠标拖尾**与**动效选择**两个分区。

---

## 二、功能模块总览

本工具共 12 个功能页面，左侧导航切换。系统概览页的内存、磁盘图标可一键跳转到对应清理页面。

| 页面 | 说明 |
| --- | --- |
| 系统概览 | 实时显示 CPU、内存、磁盘占用与开机时长，并展示本机硬件档案；点内存图标跳「内存清理」，点磁盘图标跳「磁盘清理」 |
| 磁盘清理 | 5 大类共 59 个清理项（目录 / 文件模式 / 注册表三类目标），扫描后显示风险等级与可释放空间，按需勾选清理；支持每条目「明细」预览、删除进回收站、清理后残留复查，支持一键在线更新规则库 |
| 内存清理 | 查看内存使用情况，按 6 个内存区域勾选清理（工作集/文件缓存/备用列表等，含「顽固软件专杀」）；运行进程管理为**一行式入口卡片**（左标题/中回显/右「去管理」），点击打开独立「应用进程管理」窗口 |
| 电脑优化中心 | 17 个分组共 108 个优化项，按分组展示，支持勾选部分执行（任务调度组含「顽固软件策略专杀」） |
| 启动项管理 | 扫描注册表、启动文件夹与计划任务中的开机启动项，可启停、删除、添加 |
| 右键管理 | 按 13 类扫描右键菜单项，可启停、备份并删除，操作前自动备份 |
| 磁盘测速 | 测试顺序读写、4K 随机读写、IOPS 与延迟，可调块大小、队列深度与线程数 |
| 网络测速 | 通过外部测速站点（SpeedTest.cn / 中国科大）测试真实网络带宽 |
| 实时网速 | 常驻监控网卡上下行速率与丢包率，进入页面采集、离开自动停止；支持记录数据生成折线图报告 |
| 系统还原点 | 电脑优化中心右上角「系统还原点」按钮打开弹窗，查看保护状态、创建还原点，优化出问题后可回退 |
| 操作日志 | 查看与导出全部操作记录 |
| 设置 | 功能入口（安装路径绑定 / 大模型管理 / 字体选择）、显示与主题、动效设置、系统信息、关于与说明 |

---

## 三、操作流程

### 1. 磁盘清理

1. 进入「磁盘清理」页面，点击 **扫描大小**，等待扫描完成；
2. Windows 系统分类采用二级目录（过时文件 / 系统相关 / 缓存文件 / 应用程序 / 临时文件），可展开后单独勾选；
3. 勾选要清理的项目，点击 **开始清理** 执行；执行前可在工具栏选择：
   - **强制删除**：跳过被占用文件的重试，最大程度清空；
   - **自动重建目录**（默认开）：清理后重建空目录，避免依赖目录的应用出错；
   - **删除进回收站**：文件移入系统回收站可还原（注册表项与 DISM 不适用，仍直接执行）；
4. 每个条目行尾可用 **明细** 按钮预览将删除的具体文件清单（只读，最多展示 600 条，可一键复制完整清单）；清理完成后会提示仍有残留的条目（文件被占用、需关闭相关程序或重启后再试）；
5. 风险等级说明：低风险可直接清理；**高风险项（共 2 项：Windows Installer 补丁缓存、DISM 组件清理）必须勾选「强制删除」才会执行**，请逐项核对。

覆盖内容：Windows 系统缓存与临时文件、显卡缓存（NVIDIA / AMD / Intel）、浏览器缓存（Chrome / Edge / Firefox / QQ浏览器，**通配全部用户 Profile**）、应用缓存（Steam、Epic、Office、VS Code、JetBrains、npm / pip / NuGet、网易云音乐、微信、QQ、抖音）、注册表隐私项（最近文档记录 / 运行历史 / 名称缓存）以及 QQ 与微信文件目录清理。

**规则库特性**：

- **在线更新**：工具栏「更新规则库」按钮从发布源下载最新规则（GitHub → jsDelivr → 镜像三级回退），经尺寸 / 结构 / 版本校验（防降级）后原子写入数据目录，立即生效；
- **自定义源与私有仓库**：默认源指向开源仓库 main 分支；仓库私有导致 HTTP 404 时，可在 `%APPDATA%\Trim\cleanup\` 放 `update-source.json` 指定可访问 URL 与请求头（如 `{"urls":["https://..."],"headers":{"Authorization":"Bearer <token>"}}`）；开发机（应用在 git 仓库内）可经本机 git 凭据直取远程；
- **安装路径联动**：「设置-安装路径绑定」的扫描会读取当前生效规则库的候选目录，规则库更新后扫描结果自动同步；
- **安装检测**：目标应用未安装的条目（如未装 Firefox）扫描时自动隐藏；
- **进程保护**：声明了依赖进程的条目（如 Chrome / explorer），扫描时在路径列显示「需关闭: xxx」琥珀色标签；**执行时若相关程序仍在运行会整项跳过并写明原因**，不再半清留残；
- **自定义规则**：把与内置同结构的 JSON 放进 `%APPDATA%\Trim\cleanup\custom\`，同名条目覆盖内置规则、新条目自动追加。

**图片预览**：文件目录清理条目右侧的 **预览 (N)** 按钮会打开**独立「图片预览」窗口**（不再占用主窗口）：支持左右方向键切换、缩放（−/+）、1:1 还原、旋转，窗口内可直接 **删除** 当前图片——删除后主窗口的文件列表与容量统计会**实时同步**。

### 2. 内存清理

1. 顶部 4 张卡片（与系统概览同款 2x2 布局，最大化时一行）实时展示：物理内存使用率（带进度条）、可用内存、页面文件使用、系统缓存；
2. **内存清理区域**共 6 项，分门别类勾选后点击 **开始清理**（或对单项点「清理该项」）：
   - 工作集、低优先级备用列表、合并物理内存页（默认勾选，安全）
   - **修改列表、备用列表（全部）为高风险**，执行前会弹红色二次确认，可能导致系统短暂卡顿；
   - **顽固软件专杀**（默认不勾选）：一次性结束 MuMu 模拟器 / 网易 UU 远程 / 抖音 / 剪映 / WPS 金山办公 / 微软电脑管家 的后台常驻与守护进程（含其前台进程，请先保存工作），执行结果右上角 Toast 并回传日志；
   - **点击区域条目**可弹窗查看该区域的详细简介（本地 + 联网 AI）；
3. **应用进程管理**：主页面「运行中的进程」为**一行式入口卡片**——左侧为标题与图标、中间为内容回显区、右侧为 **去管理** 按钮：
   - 点击 **去管理** 打开独立「应用进程管理」窗口，可与主窗口并排操作；
   - 同路径的进程自动合并为一条（参照任务管理器进程树）；**打开时默认折叠**，主进程名前有**展开箭头**，点击分组行可展开各子进程单独结束、再次点击收起；
   - 各应用图标**懒加载自其可执行文件路径**，提取失败时自动回退显示随包内置兜底图标（`Trim.ico`）；
   - 支持按名称或 PID 搜索、手动刷新，结束进程前有二次确认；
   - **结果实时回显**：窗口内结束进程后，最新进程统计会即时回显到主页面卡片的中间内容区；
4. 清理完成后显示释放的内存量，并自动刷新内存信息。

> 说明：内存清理调用 Windows Native API（`NtSetSystemInformation`，参考 Mem Reduct），**需要管理员权限**；无管理员权限时清理会失败并提示提权。释放量取决于当前系统状态，通常为已用内存的 10%–50%。

### 3. 电脑优化中心

1. 进入「优化电脑」页面，各优化项按 17 个分组展示为卡片；
2. 点击卡片查看详情，含执行步骤、风险等级、优缺点分析；
3. 点击 **立即执行** 前，应用会自动检查系统还原点：未创建 → 弹窗建议先创建；5 天内已创建 → 右上角轻量提醒；
4. 支持**勾选部分卡片**后点 **执行所选优化**，或点 **全选执行** 一次执行当前页全部项目；
5. **高危安全操作**（禁用 UAC、关闭 Defender、禁用 70+ 服务、BCD 超优化等）执行前会弹**红色警示二次确认**；
6. **任务调度-顽固软件策略专杀**（无还原）：一次性将 MuMu 模拟器 / 网易 UU 远程 / 微软电脑管家 等后台常驻服务的启动类型改为「手动」并立即停止，同时停止 WPS 云文档服务、删除 WPS 更新计划任务并关闭其自动升级；
7. 执行过程中有实时进度条，完成后 Toast 提示。

> **重要提醒**：108 个优化项中 14 项为高风险，其中 **13 项执行后无法一键还原**（仅「MMCSS 系统增强」提供还原步骤）。涉及服务、驱动、注册表与启动参数的改动，请务必**先创建系统还原点**再执行。

### 4. 系统还原点管理

在「电脑优化中心」页面右上角点击 **系统还原点** 打开弹窗，可查看各磁盘的保护状态、还原点数量与最近还原点；点击 **创建还原点** 为所有已启用保护的磁盘创建还原点。优化导致异常时可在系统设置中执行系统还原回退。

### 5. 启动项管理

1. 进入「启动项管理」页面自动扫描（注册表 Run/RunOnce、启动文件夹、登录与开机计划任务）；
2. 勾选后可执行 **启用 / 禁用**（可逆）或 **删除**（先备份，不可逆）；
3. 可 **打开所在位置** 查看文件，或手动 **添加** 启动项（同名会提示冲突，不会静默覆盖）；
4. **点击启动项条目**（非按钮区域）即可弹窗查看详细简介（本地 + 联网 AI）。

### 6. 右键管理

1. 进入「右键管理」页面，按 13 类自动扫描：文件、EXE文件、LNK文件、目录、文件夹、驱动器、回收站、目录背景、桌面背景、此电脑、库、发送到、UWP应用；
2. 交互方式：**勾选 = 启用，取消勾选 = 禁用**（可随时改回）；
3. 行内 **备份并删除** 为不可逆操作，删除前会自动备份注册表项与「发送到」文件；
4. 左侧另有聚合视图（全部 / 磁盘分区 / 新建菜单 / 打开方式 等），便于按场景筛选；
5. 点击条目可查看详情，并可调用联网 AI 查看简介。

### 7. 磁盘测速

设置测试路径（默认自动检测当前用户的「下载」目录）、块大小、队列深度与线程数后点击开始。测试会在目标目录创建临时文件，完成后自动删除。支持保存历史记录并对比。

### 8. 网络测速 / 实时网速

**网络测速**：点击开始后在测速页面选择 **SpeedTest.cn** 或 **中国科大** 节点测试真实带宽。本模块需要联网，测速数据由对应站点提供。

**实时网速**：进入页面即开始采集，显示上下行速率曲线与丢包率，可暂停、切换网卡；离开页面自动停止采集。点击右上角 **记录数据** 开始采集，再次点击 **停止记录** 自动生成报告（含每秒网速折线图），保存到 `%APPDATA%\Trim\cache\realtime-reports\`；报告保留 **7 天自动清理**。

### 9. 设置

- **功能入口**：三张入口卡片（安装路径绑定 / 大模型管理 / 字体选择）固定一行排列，点击各自按钮打开统一样式的弹窗或独立窗口；
- **安装路径绑定**：自动扫描并绑定各软件安装路径与文件目录，弹窗内分门别类管理（安装路径 / 文件目录 / 缓存目录，带软件真实图标）；
- **大模型管理**：打开**独立「大模型管理」窗口**，按分组配置四个模型项的启用状态、接口地址、密钥与模型名；
- **字体选择**：内置 MiSans 可变字体（字重 100–1000 无级调节），支持系统字体与自定义字体导入；字重 / 字号滑块先在弹窗内实时预览，点 **应用** 后全局生效；配置保存在 `%APPDATA%\Trim\settings.json`；
- **显示**：
  - **主题选择**：跟随系统 / 浅色 / 深色；下方「预设主题选择」切换内置渐变墙纸，「主题颜色选择」自定义界面强调色；
  - **窗口材质**：Mica / Mica Alt / 亚克力 / 细亚克力四种原生材质卡片，右上角**总开关**关闭即恢复无材质（不透明）观感，材质同日作用于主窗口与三个子窗口；
  - **背景图片**：导入图片作为主页面背景，可调**雾化度**（图片与界面融合的朦胧程度）与**背景模糊度**（0–100% 无级，即液态玻璃面板强度），支持多图管理与一键跳转存放目录；
  - **动效设置**：**鼠标拖尾**开关（仅当前窗口生效）与**动效选择**（完整 / 标准 / 磨砂 / 关闭四档液态玻璃强度）；
- **系统信息**：应用版本、Electron、操作系统等运行环境信息（默认折叠）；
- **关于与说明**：点击「查看说明」弹出本文档。

> 全应用弹窗分两类：**轻量确认/表单类弹窗**共用同一套三段式弹窗骨架与样式；**独立窗口类**（应用进程管理 / 大模型管理 / 图片预览）为独立 BrowserWindow，可与主窗口并排显示。

### 窗口尺寸

主窗口默认并最小为 **1294×870（客户区渲染，`devicePixelRatio = 1`）**，可自由拉伸放大或最大化，**不允许缩小**到该尺寸以下。

### 独立窗口显示（无黑闪）

「应用进程管理 / 大模型管理 / 图片预览」三个独立 BrowserWindow 均采用 `show: false` + `ready-to-show` 页面首帧渲染完成后才显示，从根源消除打开瞬间的背景色空帧黑闪。

---

## 四、联网 AI 简介怎么用

启动项管理、右键管理、内存清理（区域点击弹窗内）提供「简介」能力，采用**本地简介 + 联网 AI 简介**两层设计：

- **本地简介**：随程序内置，打开详情立刻显示，**不联网、不上传任何信息**；
- **联网 AI 简介**：只有当你再点击一次 **获取 AI 简介** 时才会发起请求，且只上传「项目名称 + 厂商/分组」，不包含任何注册表路径、文件路径或账号信息；
- 各模块的 AI 简介**互不联动**：各自选择模型、各自缓存（7 天有效）；
- 若所选模型尚未启用，点击「获取AI简介」会**直接打开「大模型管理」独立窗口**引导你启用。

### 配置模型

在 **设置 → 大模型管理 → 去管理** 中配置，共四个模型项：百度千帆（web_summary 接口）、知乎直答（OpenAI 兼容）、秘塔 AI（OpenAI 兼容）、自定义模型（OpenAI 兼容 chat/completions 协议）。

**所有内置模型的密钥默认留空、默认关闭。** 填写密钥后点保存，程序会自动发一条测试消息；校验通过才会启用。密钥保存在本机 `%APPDATA%\Trim\settings.json`，不会上传。

---

## 五、安全注意事项

1. 清理、右键菜单修改、启动项删除，建议保持**二次确认开启**；
2. **执行高风险优化项前务必先创建系统还原点**，其中 13 项无法一键还原；
3. 删除类操作（右键项、启动项、文件清理）执行前会自动备份，但**备份不等于还原点**，涉及系统级改动仍建议先建还原点；
4. 磁盘清理中的高风险项（Windows Installer 补丁缓存、DISM 组件清理）影响系统更新卸载能力，请确认理解后再勾选「强制删除」；
5. 网络测速与联网 AI 简介会产生外网访问，其余功能均在本机完成；
6. 本工具仅供个人测试交流使用，使用风险自负。

---

## 六、日志与反馈

- 所有操作均记录于 `%APPDATA%\Trim\logs\`，可在「日志」页面查看与导出；
- 配置与缓存存放于 `%APPDATA%\Trim\`（settings.json、paths.json、cache）；
- 如有问题或建议，欢迎联系：1607642197@qq.com
