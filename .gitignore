# 依赖
node_modules/

# 构建产物
build-release/
build-release-new/
build-release-v44/
"Trim goujian/"
dist/
*.exe
*.blockmap

# 安装器编译缓存
trim-installer/

# Rust 构建缓存（finder.exe 构建后由 extraResources 取件，不入库）
native-scanner/target/

# 内置 PowerShell7 运行时发行包（>100MB 超 GitHub 单文件上限，仅本地取件打包，必须不入库）
vendor/pwsh/*.zip
vendor/pwsh/*.zip.sha256

# 智能体工作目录
.workbuddy/

# 方案文档（本地资料，不入库）
docs规范/

# 日志与临时文件
*.log
.tmp-ps-syntax-check.ps1

# 编辑器与工具
.vscode/
.vexor/

# 更新历史（本地资料，不入库）
update history/

# 本地 AI 工具产物与个人档案（v3.6.5 新增）
# USER.md / IDENTITY.md 含开发者个人信息与协作偏好，公开仓库不得入库
# AGENTS.md 含本机绝对路径与智能体归档布局，属环境强绑定文档，同样不入库、不 pull、不构建
# 说明见 AGENTS.md 第 8 节「智能体工作目录归档」
.opencode/
config/
IDENTITY.md
USER.md
AGENTS.md
