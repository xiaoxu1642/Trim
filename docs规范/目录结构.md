

TuneForge/

├── 【根目录：Electron 三件套 + 工程文件】

│   ├── main.js              主进程（4620 行）：5 窗口、全部 IPC、pwsh 调度、原生材质、规则更新、删除协议

│   ├── preload.js           预加载：32 个 window.api 命名空间的白名单转发（渲染层唯一系统入口）

│   ├── test-features.js     npm test 无头自检：语法/规则双源一致/PS AST/页面挂载/1294×870 常量断言

│   ├── package.json         只产 portable 包；files 白名单 + finder.exe extraResources

│   ├── AGENTS.md            AI 协作红线（精简版）

│   ├── readme.md            用户说明，同时是应用内「使用说明」弹窗的唯一数据源（会打进包）

│   └── LICENSE.md / .gitignore / package-lock.json

│

├── scripts/                 【开发期工具，运行时不用】

│   ├── gen-fallback.js      cleanup-rules.json → 生成渲染层兜底（prebuild 自动跑）

│   ├── sign-rules.js       规则库 ed25519 签名/密钥生成（发布前手动跑）

│   └── fix\_icons.py         source.png 母版 → 全套 png/ico，并同步 Tauri 安装器图标

│

├── src/                     【应用全部源码】

│   ├── \*.html（5 个）        index 主窗口 + 4 个独立窗口（大模型/外设/进程管理/图片预览）

│   ├── main/                主进程共享 Node 模块（被 main.js、测试、签名脚本 require）

│   │   ├── security.js      原子写 JSON、DPAPI 密钥加解密、损坏配置隔离

│   │   ├── rules-signature.js  ed25519 内置公钥与验签

│   │   └── diag.js          失败四元组诊断

│   ├── scripts/             渲染层 39 个脚本（不碰 Node），分四类：

│   │   ├── 基建   app/modal/logger/ds/xtable/icon-fallback/intro

│   │   ├── 外观   theme/theme-boot/pathbinding/window-material/liquid-glass/spotlight/mouse-trail/tilt

│   │   ├── 功能页 cleanup/finder/optimizer/startup/contextmenu/memoryclean/processes/… 共 18 个

│   │   ├── 子窗口 models-window/peripheral-window/process-manager-window/preview-window

│   │   └── cleanup-fallback.generated.js  ← 生成物，禁止手改

│   ├── scripts-powershell/  主进程侧 13 个 PS 脚本"生成器"（导出字符串，不碰 DOM）

│   │   └── 最大三个：optimizer-scripts 2171 行（108 优化项）、cleanup 979、contextmenu 703

│   ├── styles/              main.css（7463 行，token+分层覆盖，先加载）→ ds.css（设计系统，后覆盖）

│   ├── data/                cleanup-rules.json（59 清理项唯一真源）+ item-intro.json（本地简介）

│   └── assets/              fonts/MiSansVF.ttf（asarUnpack）+ ico/（唯一图标目录，Trim.ico 全场景兜底）

│

├── native-scanner/          【Rust 子工程】编译 finder.exe：重复/大文件/空文件扫描+删除

│   ├── src/main.rs          1004 行；target/release/finder.exe 是打包取件处（target 不入库、本地保留）

│   └── Cargo.toml/Cargo.lock

│

└── docs规范/                【项目文档】（注意：architecture 内文自称 docs/，实际目录是中文名）

&#x20;   ├── architecture.md      ★架构事实+第十节回归陷阱库，最权威

&#x20;   ├── design-system/       ds 设计规范与 API（README + tuneforge-2/MASTER）

&#x20;   └── update/              历史快照不改写：9,4（重设计/安全设计）、9.6（侧边岛、黑闪、上轮审查报告）



