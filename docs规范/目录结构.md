

Trim/

├── 【根目录：Electron 三件套 + 工程文件】

│   ├── main.js              主进程（约 5490 行，v2.6.0）：5 窗口、全部 IPC、pwsh 调度、原生材质、规则更新、优化中心记账/回读、便携模式

│   ├── preload.js           预加载：约 35 个 window.api 命名空间的白名单转发（渲染层唯一系统入口）

│   ├── test-features.js     npm test 无头自检（96 项断言）：语法/规则双源一致/PS AST/页面挂载/记账与迁移/体检/启动页/关闭即隐

│   ├── package.json         版本唯一真源；NSIS+Portable 双 target；build.directories.output = build-release/（仓库根）

│   ├── AGENTS.md            AI 协作红线（精简版）

│   ├── readme.md            用户说明，同时是应用内「使用说明」弹窗的唯一数据源（会打进包）

│   └── LICENSE.md / .gitignore / package-lock.json

│

├── scripts/                 【开发期工具，运行时不用】

│   ├── gen-fallback.js      cleanup-rules.json → 生成渲染层兜底（prebuild 自动跑）

│   ├── sign-rules.js        规则库 ed25519 签名/密钥生成（发布前手动跑）

│   ├── build-fastsize.ps1   TrimFastSize.dll 构建脚本（C# → extraResources fastsize/）

│   ├── gen-installer-assets.py  NSIS 安装器品牌位图生成（输出 build/installer/*.bmp）

│   ├── clean-old-packages.js  postbuild 钩子：构建后自动把非当前版本旧安装包移入回收站

│   └── fix_icons.py         source.png 母版 → 全套 png/ico

│

├── src/                     【应用全部源码】

│   ├── *.html（5 个）        index 主窗口 + 4 个独立窗口（大模型/外设/进程管理/图片预览）

│   ├── main/                主进程共享 Node 模块（被 main.js、测试、签名脚本 require）

│   │   ├── security.js      原子写 JSON、DPAPI 密钥加解密、损坏配置隔离

│   │   ├── rules-signature.js  ed25519 内置公钥与验签

│   │   ├── optimization-state.js  v2.6.0 优化项「已应用状态」记账（fail-closed：先记账后执行、还原成功才销账）

│   │   ├── version-migrations.js  v2.6.0 退役优化项迁移（启动按备份原值自动还原，失败保留重试）

│   │   ├── updater.js       v2.6.0 自动更新（electron-updater + GitHub/gh-proxy 系多线路顺序回退 + 镜像偏好持久化）

│   │   ├── ps-protect-path.js / ps-rule-path-eval.js  受保护路径清单 / 受限路径表达式求值器（JS+PS 单一来源）

│   │   └── diag.js          失败四元组诊断

│   ├── scripts/             渲染层 41 个脚本（不碰 Node），分四类：

│   │   ├── 基建   splash（启动页，body 末尾脚本第一个加载）/app/modal/logger/ds/xtable/icon-fallback/intro

│   │   ├── 外观   theme/theme-boot/pathbinding（含壁纸轮换引擎）/window-material/liquid-glass（噪点层+环境自适应）/spotlight/mouse-trail/tilt

│   │   ├── 功能页 overview（系统体检）/cleanup/finder/optimizer/startup/contextmenu/memoryclean/updater-ui/… 共 20 个

│   │   ├── 子窗口 models-window/peripheral-window/process-manager-window/preview-window

│   │   └── cleanup-fallback.generated.js  ← 生成物，禁止手改

│   ├── scripts-powershell/  主进程侧 13 个 PS 脚本"生成器"（导出字符串，不碰 DOM）

│   │   └── 最大三个：optimizer-scripts（125 优化项 + effect 分级）、cleanup、contextmenu

│   ├── styles/              main.css（约 8100 行，token+分层覆盖，先加载）→ ds.css（设计系统，后覆盖）

│   ├── data/                cleanup-rules.json（清理项唯一真源）+ item-intro.json（本地简介）+ retired-optimizations.json（v2.6.0 退役清单）

│   └── assets/              fonts/MiSansVF.ttf（asarUnpack）+ ico/（唯一图标目录，Trim.ico 全场景兜底）+ bg/（v2.8.0 内嵌实拍壁纸 wp-*.jpg）

│

├── native-scanner/          【Rust 子工程】编译 finder.exe：重复/大文件/空文件扫描+删除

│   ├── src/main.rs          target/release/finder.exe 是打包取件处（target 不入库、本地保留）

│   └── Cargo.toml/Cargo.lock

│

├── build-release/           【构建产物，gitignore】NSIS Setup / Portable exe + latest.yml + blockmap（GitHub Releases 发布件）

│

└── docs规范/                【项目文档】（注意：architecture 内文自称 docs/，实际目录是中文名）

    ├── architecture.md      ★架构事实+回归陷阱库，最权威

    ├── 目录结构.md           本文件

    ├── design-system/       ds 设计规范与 API（README + tuneforge-2/MASTER）

    └── update/              历史快照不改写：9,4 / 9.6 / 9.7 / 9.11 / 9.12 / 9.13（按日期归档）

